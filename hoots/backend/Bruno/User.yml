info:
  name: User
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/:userId"
  params:
    - name: userId
      value: sss
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
