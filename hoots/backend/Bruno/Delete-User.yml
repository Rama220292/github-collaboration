info:
  name: Delete-User
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{BASE_URL}}/:userId"
  params:
    - name: userId
      value: sss
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
