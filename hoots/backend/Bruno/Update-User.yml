info:
  name: Update-User
  type: http
  seq: 3

http:
  method: PUT
  url: "{{BASE_URL}}/:userId"
  params:
    - name: userId
      value: sss
      type: path
  body:
    type: json
    data: |-
      {
        "username": "xxx",
        "hashedPassword": "999"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
