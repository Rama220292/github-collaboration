info:
  name: Create-User
  type: http
  seq: 2

http:
  method: POST
  url: "{{BASE_URL}}/userId"
  body:
    type: json
    data: |-
      {
        "username": " ",
        "hashedPassword": "999"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
